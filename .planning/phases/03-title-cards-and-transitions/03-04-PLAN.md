---
phase: 03-title-cards-and-transitions
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified: []
autonomous: false
requirements:
  - COMP-02
  - TRAN-02
  - TRAN-03

must_haves:
  truths:
    - "User can add a title card, see it in the frame strip, edit its text and colors, and see the preview render it correctly"
    - "User can select crossfade and the preview shows smooth alpha-blend between frames"
    - "User can select slide-left or slide-right and the preview shows the correct directional slide"
    - "Exported GIF with title cards and transitions matches what the user saw in the preview"
    - "Title cards are draggable and reorderable in the frame strip"
  artifacts:
    - path: "src/components/TextFrameThumbnail.tsx"
      provides: "Verified: visible in frame strip, draggable, shows background color and text"
    - path: "src/components/PreviewPlayer.tsx"
      provides: "Verified: smooth transition animation visible in canvas preview"
    - path: "src/components/ExportPanel.tsx"
      provides: "Verified: exported GIF contains smooth transitions, file size estimate is reasonable"
  key_links:
    - from: "Preview canvas"
      to: "Exported GIF"
      via: "Visual identity — both use same expanded sequence logic"
      pattern: "preview matches export"
---

<objective>
Human verification of the complete Phase 3 feature set. Confirms that title cards and transitions work end-to-end in both the live preview and exported GIF.

Purpose: Phase 3 success criteria require visual verification — automated tests cannot confirm smooth animation, correct color rendering, or GIF visual output.
Output: User confirms all seven verification checks. Phase 3 complete.
</objective>

<execution_context>
@/Users/danielwilliams/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielwilliams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and verify build</name>
  <files></files>
  <action>
Run the development server and confirm the build is clean before human verification:

```bash
npm run build 2>&1 | tail -20
```

If the build fails, fix TypeScript or build errors before proceeding to the checkpoint.

Then start the dev server:
```bash
npm run dev
```

Note the local URL (typically http://localhost:5173).
  </action>
  <verify>npm run build exits 0</verify>
  <done>Build succeeds with zero errors. Dev server is running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — title cards and transitions</name>
  <action>
Pause for human to visually verify the complete Phase 3 feature set in the browser. The automated build in Task 1 confirms no compile errors; this checkpoint confirms the features work correctly from a user perspective.
  </action>
  <verify>User confirms all 7 visual checks pass by typing "approved"</verify>
  <done>All 7 checks pass: title cards visible and editable, transitions animate correctly in preview, exported GIF matches preview</done>
  <what-built>
Complete Phase 3 feature set:
- Title card frames (TextFrame) with background color, text, and editing controls
- TextFrameThumbnail in the frame strip (sortable, selectable)
- EditPanel text frame controls (text textarea, background color, text color, font size)
- Crossfade transition (alpha blend) in preview and export
- Slide-left and slide-right transitions in preview and export
- File size estimate updated for transition frames
  </what-built>
  <how-to-verify>
Open http://localhost:5173 and run through these 7 checks:

**Check 1 — Add Title Card:**
- Upload 2-3 image files (drag-drop or click)
- Click "Add Title Card" button in the left sidebar
- Confirm: a dark-blue tile labeled "Title Card" appears in the frame strip
- Confirm: EditPanel on the right shows text/background/text color/font size controls

**Check 2 — Edit title card:**
- In EditPanel, type "Portfolio" in the text field
- Confirm: the frame strip thumbnail updates to show "Portfolio"
- Change the background color to a bright color (e.g., red)
- Confirm: thumbnail background color updates immediately
- Click Play — confirm: title card frame renders in the preview canvas with the correct background color and centered text

**Check 3 — Title card reorder:**
- Drag the title card thumbnail in the frame strip to a different position
- Confirm: it reorders without errors, preview reflects new order

**Check 4 — Crossfade transition:**
- In Export Settings, change Transition from "Cut (instant)" to "Crossfade"
- Click Play in the preview
- Confirm: preview shows smooth alpha-blend between frames (not instant cuts)
- Stop playback

**Check 5 — Slide transitions:**
- Change Transition to "Slide Left"
- Click Play — confirm: frames slide in from the right (directional slide, not alpha fade)
- Change Transition to "Slide Right"
- Click Play — confirm: frames slide in from the left

**Check 6 — Export with transitions:**
- Set Transition to "Crossfade" (or any non-cut)
- Confirm: file size estimate is visible and > 0 KB
- Click "Export GIF"
- Confirm: progress bar appears, GIF downloads
- Open the downloaded GIF in a browser — confirm: smooth crossfade between frames is visible

**Check 7 — Preview matches export:**
- Visually compare the preview animation to the exported GIF
- Confirm: both show the same visual effect (same transitions, same title card appearance)
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 7 verification checks pass.
</verification>

<success_criteria>
- Title cards add, display, edit, and reorder correctly
- Crossfade, slide-left, and slide-right transitions visible in preview
- Exported GIF contains smooth transitions
- Preview and exported GIF are visually identical
- Phase 3 requirements COMP-02, TRAN-02, TRAN-03 all verified complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-title-cards-and-transitions/03-04-SUMMARY.md`

Update .planning/STATE.md:
- Current Position: Phase 3 of 3 complete
- Status: All phases complete — v1.0 milestone done
- Progress: [██████████] 100%

Update .planning/ROADMAP.md:
- Mark Phase 3 as complete with date
- Mark all three plans as complete ([x])
</output>
